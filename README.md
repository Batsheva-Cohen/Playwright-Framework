


# הסבר מושגים
**web-first assertion היתרון שנותן לי**
מונע בדיקות שבירות כי הוא חוסך לי את הניהול של כל מה שקרה עד שהכפתור הספציפי שאני צריכה לצורך הבדיקה נטען ועולה לדף ם 
 ולנהל ידנית את ההמתנות מה שגורם לבדיקות להיות שבירותSLEEP וזה מה שחוסך לי מלהשתמש ב 
 זאת אומרת הוא מנסה ומנסה שוב להפעיל את הבדיקה עד שמקבל את התוצאה הרצויה ואז הטסט עובר 
 במקום לנסות פעם אחת להכשל כי הכפתור לא נטען וכמובן שהחזרתיות קוראת סביב זמן מוגדר שבו מנסים 
expect וכל זה באופן אוטומטי בתוך הבדיקת 

**POM*היתרון של מודל**
הוא בעיקר בתחזוקה.
אם השתנה משהו במבנה של ה
HTML 
או שהשתנה מספר זהות או שם של לוקטור,
והטסטים שלי השתמשו והסתמכו ישירות על המבנה והמזהים שמצוינים ב 
HTML,
אז במצב של שינוי אני אצטרך לעבור בכל הטסטים שלי ולשנות כל מה שהשתמש
ישירות במבנה של ה.
POM 
חוסך לי את כל התחזוקה הזו. במקום אחד הכל מוגדר, והטסטים שולפים
את הכל מוכן משלב האתחול 
init. 
אם לוקטור משתנה, אני משנה את הקוד רק במקום
אחד באתחול וזה מה שהופך את התחזוקה ליעילה.

הבדיקות עצמן אינן צריכות להכיר לוקטורים מכיוון שהתפקיד שלהן הוא לבדוק
את הלוגיקה העסקית ואת התנהגות האפליקציה, ולא את המבנה הטכני של הדף.
הפרדה זו שומרת על קוד בדיקות קריא ונקי.

***scope function ל-scope session*ההבדל בין**
*scope session:*
מגדירה בו להיות חי לאורך כל הריצה - *fixture*  המחזור חיים של ה
הוא יוולד בטסט הראשון שאני יפעיל וישאר חי לשאר הטסטים 
*scope function:*
מבודד לכל בדיקה ובדיקה הוא חי ברגע שמפעילים אותה - *fixture*  המחזור חיים של
ומת ברגע שמסתיימת
מכיוון שעלות הרמת השרת היא גבוה ולוקחת הרבה זמן *seseon* הפעלה השרת היא תהיה במצב של
אני לא רוצה בכל בדיקה מחדש להפעיל את השרת אני מפעילה פעם אחת וכל הבדיקות רצות על אותו שרת
 כדי למנוע התנגשויות בן הבדיקות *function* ואילו ניקוי משימות תהיה במצב 
 כלומר אני רוצה שכל בדיקה תהיה מבודדת מבדיקה אחרת ולא עם שאריות של מידע בן בדיקה לבדיקה 
 ולכן אני רוצה מצב מבודד ללא שאריות מידע וללא התנגשויות
 

**איך בדיקות API קשורות לבדיקות UI**
בדיקות ה-API הן בדיקות שניגשות ישירות לשרת ומדברות איתו ישירות, בלי לעבור דרך הדפדפן ובלי לראות פיזית את המידע מוצג על המסך.
לעומת זאת, בדיקות UI הן בדיקות שבהם אנחנו כן פועלים פיזית על המידע והכפתורים שמוצגים על המסך כמו בחלק 1 עם כל הלוקטרים. מה שקורה מאחורי הקלעים בפעולות האלה זו תקשורת מול השרת כדי שיבצע את השינויים. אם השרת יחזיר סטטוס קוד לא תקין ה-UI לא יצליח לעדכן את המסך, הפעולות שרצינו שיקרו לא יופעלו, והבדיקה שלי ב-UI תיכשל

## SMOKE הרצת בדיקות
בדיקות אלו הן הבדיקות הקריטיות והמהירות ביותר בפרויקט, שנועדו לוודא שהמערכת הבסיסית חיה ונושמת כמו טעינת דף הבית או התחברות עם טוקן תקין
כדי להריץ את קבוצת בדיקות ה-Smoke בלבד, יש להריץ את הפקודה הבאה בטרמינל:
*the command:*
uv run pytest -m smoke

**Playwright Trace Viewer**
שבאה לבדוק שאכן המשימות מסומנות כבוצעוtest_test_mark_task_as_done בחרתי בבדיקה 
done בעוד שהם to do שיניתי אותם כמצפות לקבל מצב 
והופיע לי הטבלה הנוכחית עם סטטוס של בוצע Playwright Trace Viewerזה נכשל פתחתי את ה
done בעוד שזה todo ככה שאני מבינה שזה נכשל כי ציפיתי לקבל 


מהם היתרונות של Playwright על פני כלים ותיקים יותר. יש להזכיר auto-waiting, תמיכה במספר דפדפנים, ובדיקות API מובנות.
מהי web-first assertion ומהו auto-waiting, ואיך הם מפחיתים בדיקות שבירות.
מהי אסטרטגיית בחירת locators, ומדוע get_by_role ו-data-testid עדיפים על שליפה לפי מבנה ה-DOM או class.
מהו Page Object Model, ואיזו בעיית תחזוקה הוא פותר.
מהו fixture ב-pytest, ומה ההבדל בין scope function ל-scope session. יש לתת דוגמה לכל אחד מה-framework שנבנה.
כיצד בודקים API ב-Playwright, ומדוע כדאי לבדוק את שכבת ה-API בנפרד מה-UI.
מהן הסיבות הנפוצות לבדיקות שבירות, flaky tests, ואיך מתמודדים איתן.
מדוע בידוד בין בדיקות חשוב, ואיך הושג כאן בידוד דרך ניקוי מצב לפני כל בדיקה.
מהי הרצה מקבילה של בדיקות, ומה צריך לקחת בחשבון כשמריצים בדיקות במקביל.
מהו trace ב-Playwright, ואיך הוא עוזר בחקירת כשל.
כיצד הייתם בונים תשתית אוטומציה מאפס. יש לתאר את השכבות, הפרדה בין pages, utils, fixtures, ובדיקות, ואת הסיבה לכל אחת.
מה ההבדל בין בדיקת UI, בדיקת API, ובדיקת end to end, ומתי מתאים כל סוג. יש להזכיר את רעיון פירמידת הבדיקות.


### שאלות ראיון עבודה
1.*מהם היתרונות של Playwright על פני כלים ותיקים יותר. יש להזכיר auto-waiting, תמיכה במספר דפדפנים, ובדיקות API מובנות.*
* מנגנון Auto-waiting זה מנגנון אוטומטי שפותח את הבעיה שהטסט מתחיל לפני שהדף ןהאלמנטים שלו עלו - והבדיקה נכשלת כי לא חכתה מספיק זמן שהדף יטען בPLAYWRIGHT יש מנגון שעובד אוטומטית במקום שאני ינהל את זה כלהזמן עם SLEEP הכל נעשה אוטומטית על ידו מה שמונע את כל הנפילות טסטים על המתנה לא מספקת 
* יכולה לתמוך במספר דפדפנים שהם מגייעים כבר מובנים ואני לא צריכה לבדוק את סדרת הבדיקות 
על דפדפנים שונים ולהתעסק בהתקנות שלהם כי זה מריץ באופן אוטומטי על דפדפנים שונים
ולא צריך להתאים את הקוד לדפדפן הספיציפי כי הכל נעשה אוטומטי 
*בדיקות API מובנות שליחת בקשות HTTP מכל הסוגים GET POST PATCH PUT DELETE ובדיקת אימות לפי סטטוס קוד תוכן תגובה זמני תגובה כותרות (Headers) 

2. *מהי web-first assertion ומהו auto-waiting, ואיך הם מפחיתים בדיקות שבירות.*
 זה המנגנון שאחראי על ההמתנה בזמן האימות הוא מחכה שהכפתור יעלה ויהיה זמין כדי לבצע עליו אצ הפעולותweb-first assertion ה 
 ולנהל את ההמתנות בעצמיאבל הוא לשלב אחרי האימות sleep זה המנגנון המתנה שגם הוא חוסך לי מלכתוב auto-waiting ואילו ה 
 ששם הוא מחכה שהכפתור יהיה זמין שלא יזוז שלא יהיה איזה משהו שמכסה אותוclik בשביל פעולות שקורות בטסט עצמו כמו 
 יש לו גם כן זמן המתנה עם גבול מסויים ורק אז מבצע את הבדיקה שלו 
 הוא בכל הזמן המתנה שמוגדר לו מנסה להפעיל אבל לא מפיל את הטסטeb-first assertion שניהם מפחיתות בדיקות שבירות כי ה 
pass עד שהוא מצליח ומחזיר לי
sleep שהוא מיועד לבדיקות שהן פעולות וכל זה חוסך לי את auto-waiting ווכנל גם 
שאני צריכה לנהל בעצמי וגורם לטסטיםליפול כי האלמנטים עוד לא מוכנים וכו

3.*ואיזה בעיית תחזוקה הוא פותרPage Object Model מהו*
זה תבנית עיצוב שמפרישה בן איפה האלנטים נמצאים לאיך שולפים אותם ואיפה שבודקים אותם 
אז במצב של שינוי שמות זהות או שם של locatorim אם הטסטים שלי השתמשו ישירות ב 
משהו שם אני אצטרך לעבור ידנית על כל הטסטים שלי לשנות ידנית אותם בהתאם לשינוי
לעומת זאת כשהתפקידים תחת חלוקה ומבודדים אז כשמשהו משתנה הוא מוגדר במקום אחד ורק שם אנ משנה וכל הטסטים עובדים לי ללא שנגעתי בהם
וזה מקל מאוד על התחזוקה 

4.*מהו fixture ב-pytest, ומה ההבדל בין scope function ל-scope session. יש לתת דוגמה לכל אחד מה-framework שנבנה.*
הוא מכין לי את הקרקע לפני שמתחילה לבדוק תכלס את הטסט עצמו כמו לפתוח שרת להתחבר לבסיס נתונים וכו 
וזה במקום לשכפל קוד בשביל לעשותאת הדברים האלה בכל פעם לפני טסט 
כפרמטרןלאחר שהבסיס מוכן לו הוא יתחיל את הטסט שלו fixture הטסט יקבל את ה 
 זה מצב ברירת מחדל שדואג שהפיקסטור ירוץ לי מחדש בכל פעם function ההבדל בן ההגדרות הוא ש 
 ולא ישאר לי נתונים עוד מהטסט הקודם ששינו דברים
 זה נותן לי בידוד מוחלט בן טסט לטסט עם זאת הוא איטי מאוד כי כל פעם מחדש צריך לפתוח מחדש הכל
 במצב הזה הפיקסטורה רצה רק פעם אחת בלבד session ואילו 
 אם 5 טסטים השתמשו בו הוא יופעל בטסט הראשון שקרא לו וישאר פתוח עבור כולם ולא יחזר על הפעולה שוב ב4 הטסטים הנוספים 
 לדוגמא -
 @pytest.fixture
def board(page: Page) -> TaskBoardPage:
    board = TaskBoardPage(page)
    board.open()
    return board

function זה המצב שלא מצויין כלום זה הברירת מחדל והוא מצב 
ואני רוצה שזה יהיה מבודד כי בכל פתיחה של דף אני רוצה דף נקי וחדש בן טסט לטסט אם ישאר לי זבל מטסט קודם הטסט הבא יכול להכשל לי


@pytest.fixture(scope="session")
def auth_token(api_request_context: APIRequestContext) -> str:
    response = api_request_context.post(
        "/auth/login",
        data={"username": settings.username, "password": settings.password},
    )
    assert response.status == 200
    return response.json()["access_token"]
 התחברות לשרת עם שם משתמש וסיסמה מתבצע פעם אחת בשביל למנוע מלא פעמים התחברות פעם אחת הוא נפתח ופה זה נגמר וזה פתוח לכל הטסטים ויש גישה 
 וככה זה מהיר יותר אבל פחות מבודד

 5. *כיצד בודקים API ב-Playwright, ומדוע כדאי לבדוק את שכבת ה-API בנפרד מה-UI.*
 זה מהיר ונוח וקל לבדוק ולעקוב ככהAPIRequestContext בודקים אותו בעזרת הכלי המובנה שלו ה
PIRequestContext הבקשה מתבצעת ישירות מול השרת דרך הפיקסטורה המובנית של 
והבקשה מגיע ישירות לשרת מבלי להגיע לדפדפן
אני מחזיקה בשתובה בתוך משתנה ויכולה לבדוק את הרכיבים שלו ולדעת מה תוצאות הבדיקה ולבצייע את הבדיקות 
assert לפי מה שאני מצפה עם 
apiשאי אפשר לבצע דרך ui את הבדיקות אני מבודדת ומצעת בנפרד קודםכל לפעמים יש להם תפקידים שונים ויש בדיקות של 
כמו בדיקות עיצוביות 
וזה מהסביה שבזמן שבדיקה נכשלת זה מקל עלי לדכעת את הסיבה לכך uiמהיר פי כמה עדיין מבצעים גם בדיקות api ומעבר לכך למרות ש 
או שבשרת הבעיה תלוי איזה בדיקה נכשלה frontendהיא ב
ואני גם צרכה לבדוק שמעבר לזה שהכל תקין דרך השרת אני צריכה לבדוק גם שהזרימת נתונים באתר עצמו עובד כמו שצריך בסוף הלקוח
 למרות שהבדיקות בהם איטיות ויקרות אני לא יכולה בלעדיהם uiמשתמש ב 
 ויעמיס לו סתםapi ודבר נוסף זה שבדיקות קצה ותקינות קלטים במצב לא תקין אני רוצה שהוא ימנע את המשך הבדיקה ולא ישלח בקשה ל 
מגן על אבטחה גם פריצות וכוapi ואילו ה 
 לא יכולות לעשות ui מה שבדיקות 

 6. *מהן הסיבות הנפוצות לבדיקות שבירות, flaky tests, ואיך מתמודדים איתן.*
 אם הכפתור בדיוק זז או משהו כיסה אותו בדיוק והטסט כבר רץ בדיוק על הזמן הזה זה יכשל 
 לכן משתמשים בהמתנות אוטומטיות ולא מתעסקים ידנית בזה 
expect וזה מה שעשיתי עם בדיקות 
או שיש לי משימות עם שם זהה שנוצרות 2 כאלה יכולה להיות התנגשות ולכן חייב שמות יחדודיים לכל שם משימה שאני אדע להבדיל בינהם 
והיא מבצעת את היחוד של השם המשימה באלגוריתםunique_titleמה שעושים זה שולחים כל שם משימה לפונקציה 

7.*מדוע בידוד בין בדיקות חשוב, ואיך הושג כאן בידוד דרך ניקוי מצב לפני כל בדיקה.*
על ידי הפיקסטורה שלנו עם scop="function"

8.*מהי הרצה מקבילה של בדיקות, ומה צריך לקחת בחשבון כשמריצים בדיקות במקביל.*
במקום שהבדיקות ירוצו אחת אחרי שניה אני יכולה להפעיל כמה ליבות והם ירוצו מבקביל כל הבדיקות יתחלקו
בן הליבות וכל ליבה תריץ את הטסטים שלה זמן הריצה יתקצר משמעותית
אבל יש כאן סיכון של התנגשויות בן הבדיקות וחלקם יכשלו במידה ותהיה התנגשות נתונים
ואם אני עושה הרצה במקביל אני חייבת לוודא שכל בדיקה מבודדת לחלוטין 
אז אני לא יכולה מתוך טסט לחכות לתוצאה של טסט אחר כי אני חייבת בידוד מוחלט 
השרת צריך להיות חזק כדי להתמודד עם הבקשות שמגיעות אליו יחד 
ושניהם יחד יגשו לטסט x ובידוד של שמות המשימות כי אם 2 טסטים יחד יוצרים לי לצורך הבדיקה שלהם משימה בשם 
תהיה התנגשות והטסטים יכשלו ולכן השם יחודי לכל משימה זה קריטי במצב כזה 

9. *מהו trace ב-Playwright, ואיך הוא עוזר בחקירת כשל.*
מציג עקבות Playwright הוא כלי ממשק משתמש גרפי המסייע לך לחקור עקבות Playwright מוקלטות לאחר שהסקריפט הפעיל. עקבות הן דרך מצוינת לאיתור שגיאות בבדיקות שלך כאשר הן נכשלות ב-CI. אתה יכול לפתוח עקבות באופן מקומי או בדפדפן שלך בכתובת trace.playwright.dev.
ואני בעצם יכולה לראות על מה זה נפל מתיעוד של מה שקרה בטסט כולל צילומי מסך הוא גם יסמן לי באדום 
איפה ניסיתי ללחוץ ומה קרה הוא יכתוב לי מה הקטד ססטוס שחזר שמקבל לדעת מה הבעיה 
ויציג לי את המצב של המערכת בזמן הבדיקה

10. *כיצד הייתם בונים תשתית אוטומציה מאפס. יש לתאר את השכבות, הפרדה בין pages, utils, fixtures, ובדיקות, ואת הסיבה לכל אחת.*
 שם אני מחלקת את הדפים של האתר כל דך מייצג מחלקה חדשה pomאני אתחיל בשכבת ה 
locators אני מגדירה שם איפה הכפתורים נמצאים בעזרת 
והפונקציו שנדרשות לצורך אינטרקציה עם הדף כמו פונק התחברות 
אבנה את שכבת ה

utils שכבת ה 
כלליות ופונקציות עזר למיניהם לצורך התחברות לבסיס הנתוניםapi היא השכבה של כל מה שלא קשור ללוגיקה יהיה שם כל מיני קריאות 

fixture שכבת 
שמה אני מנהלת את מחזור החיים של הבדיקות שם תהיה כל ההכנה שדרוש לפני הרצת הבדיקות 
הטסטים יקבלו אותם כפרמטרים לפי מצב וזה יעזור גם כדי לבודד בדיקות או למנוע איטיות בבדיקות ולארגן את הקוד
השכבה הזאתי באמת אחראית שהבדיקות ירוצו כל אחת תחת דף חלק והיא מכינה לה את הקרקע

testשכבת ה 
הבדיקות מקבלות את הפיקסטורים לפי מצב וזה עוזר להם לקבל את כל מה שצריך לפני שמתחילות תכלס בבדיקה עצמה 
שהגדרנו pom היא קוראת לפעולות דרך השכבת 
מייעלת את תחזוקת הבדיקות והבדיקות גם יראו יפות וקריאות

11. *מה ההבדל בין בדיקת UI, בדיקת API, ובדיקת end to end, ומתי מתאים כל סוג*
api בדיקות 
הם בדיקות שהבקשות בהם נשלחות ישירות לשרת זה משמש בעיקר חבדיקות של חישובים אבטחה והרשאות גישה 
וגם לבדיקות קצה כמו קלטים מאוד גדולים טעינת קבצים כבדים 
ui בדיקות 
 ואני בודקת בהם את הלוגיקה של הדף עצמו באתר api הם בדיקות מול הדפדפן כשמאחורי הקלעים הוא ניגש גם ל 
 ואני אשתמש בזה לבדוק שהלוגיקה על הדפדפן עובדת כרואוי הכפתורים לחיצים ופועלים כפי שאני מצפה
 וגם כל מה שסביב העיצוב שהכפתורים מוצגים לי ורואים אותםוכו
end to end  בדיקות 
api זה בדיקה שמשמה תהליך שלם סביב מה שקורה באתר אם אני מתחילה בהתחברות בודקת שהתחברות תקינה יש הרשאה זה כבר 
 אני בודקת שאני מצליחה ללחוץ על הכפתור הוספת פריטים באמת הם נוספו נשמרו בבסיס נתונים ui ואז אני משלבת בדיקות 
 ואז אם יש המשך של תשלום אזה ממשיך לבדוק שהכל מוצפן כראוי התשלום מצליח וכו
 ובקיצור זה יוצא בסוף שילוב של 2 סוגי הבדיקות
 אני אמורה כמה שיותר להשתמש בבדיקות מול שרת מאשר מול הדפדפן כי הבדיקות מאוד איטיות מול הדפדפן 


